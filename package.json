{
  "name": "isolating-the-domain",
  "description": "Spring Boot Spring MVC (direct field access) Thymeleaf MyBatis SQL Mapper",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/irof/isolating-the-domain.git"
  },
  "devDependencies": {
    "cypress": "^3.1.0"
  }
}
